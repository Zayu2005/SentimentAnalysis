# 3.3 面向舆情演化分析的增量聚类与事件组织方法

## 3.3.1 技术背景与问题定义

### 3.3.1.1 问题背景

在舆情分析领域，社交媒体平台每天都会产生海量的新内容，这些内容涉及社会热点、品牌事件、公共卫生等多个领域。传统的离线批处理聚类方式存在以下问题：

1. **时效性差**：无法及时发现新产生的话题事件，通常需要等待一天或更长时间的数据积累
2. **资源消耗大**：每次聚类都需要对全量数据进行计算，随着数据增长，计算成本呈指数级上升
3. **演化追踪难**：难以追踪话题的动态演化过程，无法识别话题的萌芽、爆发、衰退等不同阶段
4. **状态管理缺失**：无法自动识别话题的生命周期状态，需要人工判断话题是否已经结束
5. **语义表示粗糙**：传统TF-IDF等方法仅基于词频统计，无法理解文本的深层语义

### 3.3.1.2 技术目标

针对上述问题，本项目设计了一套面向舆情演化分析的增量聚类与事件组织方法，该方法的核心目标是：

1. **支持流式处理**：能够处理实时或准实时的数据流，及时发现新话题
2. **高效质心更新**：通过Running Average算法实现高效的质心更新
3. **生命周期管理**：自动管理话题从产生到消亡的全过程
4. **演化追踪**：记录话题的演化快照，追踪话题热度、情感等指标的变化
5. **智能命名**：利用大语言模型为话题生成描述性名称和摘要
6. **多维分类**：利用LLM为话题生成分类标签，包括行业分类、风险等级等

### 3.3.1.3 技术挑战

1. **计算效率**：如何在保证聚类质量的前提下，提高处理效率
2. **阈值设置**：如何设置合理的相似度阈值，既不过严导致话题碎片化，也不过松导致话题混淆
3. **话题合并**：如何识别并合并相似话题，避免信息冗余
4. **状态转换**：如何设计合理的状态转换规则，准确识别话题的生命周期阶段

## 3.3.2 技术总体架构

### 3.3.2.1 分层架构设计

增量聚类与事件组织方法的总体架构分为六个层次：

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              事件组织层                                           │
│   话题生命周期管理 | 话题合并机制 | 演化快照生成 | 统计更新 | 词云生成            │
├─────────────────────────────────────────────────────────────────────────────────┤
│                              聚类引擎层                                           │
│   Single-Pass增量聚类 | Running Average质心更新 | 批量处理                        │
├─────────────────────────────────────────────────────────────────────────────────┤
│                              向量索引层                                           │
│   Faiss IndexFlatIP | 话题质心管理 | 相似度检索 | 全局相似度计算                  │
├─────────────────────────────────────────────────────────────────────────────────┤
│                              嵌入提取层                                           │
│   BERT [CLS] 嵌入 | L2归一化 | 批量处理 | GPU加速                                  │
├─────────────────────────────────────────────────────────────────────────────────┤
│                              LLM智能层                                            │
│   话题命名Agent | 话题分类Agent | 情感分析Agent                                   │
├─────────────────────────────────────────────────────────────────────────────────┤
│                              数据访问层                                           │
│   TopicEventRepo | TopicContentRepo | TopicEvolutionRepo | TopicMergeRepo        │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### 3.3.2.2 模块交互关系

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  数据采集   │────▶│  预处理模块 │────▶│ 嵌入提取模块 │
└─────────────┘     └─────────────┘     └─────────────┘
                                                 │
                                                 ▼
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  演化推演   │◀────│ 生命周期管理 │◀────│  聚类引擎   │
└─────────────┘     └─────────────┘     └─────────────┘
       │                   │                   │
       ▼                   ▼                   ▼
┌─────────────────────────────────────────────────────────────────┐
│                         MySQL 数据库                            │
│  topic_event | topic_evolution | topic_merge_log | unified_content│
└─────────────────────────────────────────────────────────────────┘
```

## 3.3.3 核心技术组件详解

### 3.3.3.1 BERT嵌入提取器（embedder.py）

#### 3.3.3.1.1 功能描述

使用预训练的中文BERT模型提取文本的语义嵌入向量，将文本转换为768维的稠密向量表示。相比传统的TF-IDF等稀疏表示方法，BERT嵌入能够捕捉文本的深层语义信息。

#### 3.3.3.1.2 模型选择

使用 `hfl/chinese-roberta-wwm-ext` 预训练模型，该模型是华为诺亚方舟实验室发布的中文RoBERTa模型，在多个中文NLP任务上具有出色的性能：

- **预训练方法**：采用Whole Word Masking（整词掩码）策略
- **模型规模**：12层Transformer，768维隐藏层，12个注意力头
- **训练数据**：超过100GB的中文文本数据

#### 3.3.3.1.3 嵌入提取流程

```
Input Text → Tokenizer → BERT Encoder → [CLS] token → L2 Normalize → 768-dim Vector
```

1. **文本预处理**：将文本送入HuggingFace Tokenizer进行分词和编码
2. **BERT编码**：通过BERT模型获取最后一层隐藏层表示
3. **[CLS]提取**：取第一个token（[CLS]）的输出作为句子级别的表示
4. **L2归一化**：对嵌入向量进行L2归一化，使内积等价于余弦相似度

#### 3.3.3.1.4 关键代码逻辑

```python
def embed_texts(self, texts: List[str], batch_size: int = 64, normalize: bool = True):
    """批量提取 [CLS] 嵌入"""
    
    all_embeddings = []
    
    for i in range(0, len(texts), batch_size):
        batch_texts = texts[i: i + batch_size]
        
        # Tokenize
        inputs = self.tokenizer(
            batch_texts,
            padding=True,
            truncation=True,
            max_length=self.max_length,
            return_tensors="pt",
        ).to(self.device)
        
        # BERT 编码
        with torch.no_grad():
            outputs = self.model(**inputs)
        
        # 取 [CLS] token 的输出
        cls_embeddings = outputs.last_hidden_state[:, 0, :].cpu().numpy()
        all_embeddings.append(cls_embeddings)
    
    embeddings = np.vstack(all_embeddings).astype(np.float32)
    
    # L2 归一化
    if normalize:
        norms = np.linalg.norm(embeddings, axis=1, keepdims=True)
        norms = np.maximum(norms, 1e-12)
        embeddings = embeddings / norms
    
    return embeddings
```

#### 3.3.3.1.5 主要参数配置

| 参数名 | 默认值 | 说明 |
|--------|--------|------|
| model_name | hfl/chinese-roberta-wwm-ext | 嵌入模型名称 |
| embedding_dim | 768 | 嵌入向量维度 |
| max_length | 128 | 输入序列最大长度 |
| batch_size | 64 | 批处理大小 |
| device | auto | 计算设备（auto/cpu/cuda） |

### 3.3.3.2 Faiss向量索引（index.py）

#### 3.3.3.2.1 功能描述

使用Facebook AI的Faiss库构建高效的向量检索索引，支持大规模话题质心的快速相似度搜索。

#### 3.3.3.2.2 索引类型选择

使用 `IndexFlatIP`（内积索引），在L2归一化条件下等价于余弦相似度搜索：

- **优势**：无需训练，创建即用，查询速度快
- **原理**：对于L2归一化的向量，内积 = 余弦相似度
- **适用场景**：中小规模向量检索（百万级以下）

#### 3.3.3.2.3 核心数据结构

```python
class FaissIndex:
    def __init__(self, dim: int = 768):
        self.dim = dim
        self._centroids: Dict[int, np.ndarray] = {}  # topic_id -> centroid
        self._id_list: List[int] = []  # Faiss 索引顺序对应的 topic_id
        self._index = faiss.IndexFlatIP(dim)  # 内积索引
```

- `_centroids`: 话题ID到质心的映射字典，用于质心更新
- `_id_list`: Faiss索引中的ID列表，用于将索引位置转换为话题ID
- `_index`: Faiss索引对象

#### 3.3.3.2.4 主要方法实现

##### 3.3.3.2.4.1 添加话题质心

```python
def add_topic(self, topic_id: int, centroid: np.ndarray):
    """添加话题质心"""
    self._centroids[topic_id] = centroid.astype(np.float32)
    self._rebuild()  # 重建Faiss索引
```

##### 3.3.3.2.4.2 搜索最近邻

```python
def search(self, query: np.ndarray, k: int = 1) -> List[Tuple[int, float]]:
    """搜索最近的话题质心"""
    if self._index.ntotal == 0:
        return []
    
    query = query.astype(np.float32).reshape(1, -1)
    k = min(k, self._index.ntotal)
    scores, indices = self._index.search(query, k)
    
    results = []
    for score, idx in zip(scores[0], indices[0]):
        if idx >= 0 and idx < len(self._id_list):
            results.append((self._id_list[idx], float(score)))
    return results
```

##### 3.3.3.2.4.3 计算所有话题对相似度

```python
def get_all_pairs_similarity(self) -> List[Tuple[int, int, float]]:
    """计算所有话题对的相似度（用于合并检查）"""
    if len(self._centroids) < 2:
        return []
    
    ids = list(self._centroids.keys())
    vectors = np.array([self._centroids[tid] for tid in ids], dtype=np.float32)
    
    # 内积矩阵 = 余弦相似度矩阵 (已 L2 归一化)
    sim_matrix = vectors @ vectors.T
    
    pairs = []
    for i in range(len(ids)):
        for j in range(i + 1, len(ids)):
            pairs.append((ids[i], ids[j], float(sim_matrix[i, j])))
    
    pairs.sort(key=lambda x: -x[2])  # 按相似度降序排列
    return pairs
```

### 3.3.3.3 增量聚类引擎（engine.py）

#### 3.3.3.3.1 功能描述

实现Single-Pass增量聚类算法，支持流式数据处理。每次处理新数据时，不需要重新对所有历史数据进行聚类，而是基于已有的话题质心进行判断。

#### 3.3.3.3.2 Single-Pass算法原理

Single-Pass（单遍）聚类是一种在线聚类算法，其核心思想是：
1. 依次处理每条待聚类文本
2. 与当前所有话题进行比较，找到最相似的话题
3. 如果相似度超过阈值，归入该话题；否则创建新话题
4. 更新话题质心

#### 3.3.3.3.3 算法流程图

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                         Single-Pass 增量聚类流程                                │
│                                                                                 │
│  1. 初始化                                                                  │
│     ┌─────────────────────────────────────┐                                   │
│     │ 加载活跃话题质心到 Faiss 索引         │                                   │
│     │ 初始化话题计数和质心缓存              │                                   │
│     └─────────────────────────────────────┘                                   │
│                │                                                                 │
│                ▼                                                                 │
│  2. 主循环：遍历每批未聚类内容                                                      │
│     ┌─────────────────────────────────────┐                                   │
│     │ 获取一批未聚类内容（batch_size条）     │                                   │
│     └─────────────────────────────────────┘                                   │
│                │                                                                 │
│                ▼                                                                 │
│  3. 嵌入计算                                                                  │
│     ┌─────────────────────────────────────┐                                   │
│     │ 对每条内容计算 BERT 嵌入向量          │                                   │
│     │ 文本优先级：标题 > 正文 > 关键词      │                                   │
│     └─────────────────────────────────────┘                                   │
│                │                                                                 │
│                ▼                                                                 │
│  4. Single-Pass 聚类                                                             │
│     ┌─────────────────────────────────────┐                                   │
│     │ for each (content, embedding):     │                                   │
│     │   ├── Faiss搜索最相似话题(k=1)      │                                   │
│     │   ├── 相似度 >= 阈值？              │                                   │
│     │   │   YES → 归入现有话题             │                                   │
│     │   │   NO  → 创建新话题               │                                   │
│     │   └── 更新话题质心（Running Average）│                                   │
│     └─────────────────────────────────────┘                                   │
│                │                                                                 │
│                ▼                                                                 │
│  5. 批量写入数据库                                                              │
│     ┌─────────────────────────────────────┐                                   │
│     │ 批量分配内容到话题                   │                                   │
│     │ 批量更新话题质心到数据库             │                                   │
│     └─────────────────────────────────────┘                                   │
│                │                                                                 │
│                ▼                                                                 │
│  6. 循环直到处理完毕                                                            │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

#### 3.3.3.3.4 Running Average质心更新

当新内容归入现有话题时，需要更新话题质心。传统的质心计算方式需要遍历话题下所有内容，计算复杂度为O(n²)。本项目采用增量滑动平均更新策略，将计算复杂度降低到O(1)：

```python
def _update_centroid_running_avg(self, topic_id: int, new_embedding: np.ndarray):
    """增量更新话题质心 (running average)"""
    
    old_centroid = self._topic_centroids[topic_id]
    n = self._topic_counts[topic_id]
    
    # 计算新的质心：滑动平均
    new_centroid = (old_centroid * n + new_embedding) / (n + 1)
    
    # L2 归一化
    norm = np.linalg.norm(new_centroid)
    if norm > 1e-12:
        new_centroid = new_centroid / norm
    
    # 更新缓存
    self._topic_centroids[topic_id] = new_centroid
    self._topic_counts[topic_id] = n + 1
    
    # 更新 Faiss 索引
    self.index.update_centroid(topic_id, new_centroid)
```

**数学原理**：

设话题原有质心为 $c$，包含 $n$ 条内容。现有一条新的嵌入向量 $x$ 加入话题，更新后的质心 $c'$ 为：

$$c' = \frac{n \cdot c + x}{n + 1} = \frac{n}{n+1}c + \frac{1}{n+1}x$$

这种方法的优点：
- **时间复杂度**：O(1)，无需遍历话题下所有内容
- **空间复杂度**：O(1)，只需保存质心和计数
- **精度损失**：当 $n$ 较大时，精度损失可忽略不计

#### 3.3.3.3.5 相似度阈值设置

| 参数名 | 默认值 | 说明 |
|--------|--------|------|
| similarity_threshold | 0.75 | 话题归属相似度阈值 |
| merge_threshold | 0.92 | 话题合并相似度阈值 |

**阈值选择原则**：
- 归属阈值（0.75）：较低，允许更多内容归入现有话题，减少话题碎片化
- 合并阈值（0.92）：较高，只合并高度相似的话题，避免误合并

#### 3.3.3.3.6 创建新话题流程

```python
def _create_new_topic(self, embedding: np.ndarray, content: Dict[str, Any]) -> int:
    """创建新话题"""
    
    # 1. 提取关键词作为占位名
    keywords_raw = content.get("keywords")
    if keywords_raw:
        if isinstance(keywords_raw, str):
            keyword_list = json.loads(keywords_raw)
        elif isinstance(keywords_raw, list):
            keyword_list = keywords_raw
        
        # 取前5个关键词拼接
        kw_names = []
        for kw in keyword_list[:5]:
            if isinstance(kw, dict):
                kw_names.append(kw.get("word", str(kw)))
            else:
                kw_names.append(str(kw))
        placeholder_name = "/".join(kw_names)
    else:
        # 使用标题截取
        title = content.get("title_cleaned") or content.get("title") or ""
        placeholder_name = title[:50] if title else f"话题-未命名"
    
    # 2. 插入数据库
    topic_id = TopicEventRepo.insert(
        event_name=placeholder_name,
        centroid_embedding=embedding,
        keywords=keywords_json,
        similarity_threshold=self.similarity_threshold,
        embedding_model=self.embedding_model,
    )
    
    # 3. 更新缓存
    self._topic_counts[topic_id] = 1
    self._topic_centroids[topic_id] = embedding.copy()
    self.index.add_topic(topic_id, embedding)
    
    return topic_id
```

### 3.3.3.4 话题维护器（maintainer.py）

#### 3.3.3.4.1 功能描述

负责话题的日常维护工作，包括话题合并、生命周期管理、统计更新、演化快照生成。

#### 3.3.3.4.2 话题合并机制

当两个话题的质心相似度超过阈值时，将小话题合并到大话题：

```python
def merge_topics(self, dry_run: bool = False) -> MergeResult:
    """合并相似话题"""
    
    # 1. 加载所有活跃话题到索引
    index = FaissIndex(dim=settings.clustering.embedding_dim)
    topics = TopicEventRepo.get_active_topics()
    index.load_from_topics(topics)
    
    # 2. 计算所有话题对的相似度
    pairs = index.get_all_pairs_similarity()
    
    # 3. 按相似度降序遍历
    for topic_a, topic_b, similarity in pairs:
        if similarity < self.merge_threshold:
            break  # 已按降序排列，后面的都不满足
        
        # 4. 小话题合并到大话题
        count_a = topic_map.get(topic_a, {}).get("content_count", 0)
        count_b = topic_map.get(topic_b, {}).get("content_count", 0)
        
        if count_a >= count_b:
            target_id, source_id = topic_a, topic_b
        else:
            target_id, source_id = topic_b, topic_a
        
        # 5. 执行合并
        # 记录合并日志
        TopicMergeRepo.insert(...)
        
        # 内容重新分配
        reassigned = TopicContentRepo.reassign_merged_topic(source_id, target_id)
        
        # 更新源话题状态
        TopicEventRepo.update_status(source_id, "merged", merged_into_id=target_id)
```

**合并策略**：
- 保留内容数量较多的话题作为目标话题
- 将源话题的内容重新分配到目标话题
- 源话题状态标记为 merged，并记录合并目标ID

#### 3.3.3.4.3 话题生命周期状态机

话题从产生到消亡经历四个阶段：

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                            话题生命周期状态机                                        │
│                                                                                      │
│   emerging ──[content_count ≥ 10]──▶ active                                         │
│      │                                   │                                          │
│      │                                   │                                          │
│      ├──[inactive_days ≥ 3]──▶ declining ──[inactive_days ≥ 7]──▶ ended           │
│      │                                                                            │
│      │                                                                            │
│      └──[合并]──▶ merged                                                            │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

**状态说明**：

| 状态 | 含义 | 转换条件 |
|------|------|----------|
| emerging | 萌芽期 | 话题刚产生，内容数量 < 10 |
| active | 活跃期 | content_count >= 10 |
| declining | 衰退期 | 超过 inactive_days（默认7天）无新内容 |
| ended | 终结期 | 超过 ended_days（默认30天）无新内容 |
| merged | 已合并 | 被合并到其他话题 |

**生命周期更新算法**：

```python
def update_lifecycle(self):
    """更新话题生命周期状态"""
    
    topics = TopicEventRepo.get_active_topics()
    now = datetime.now()
    
    for topic in topics:
        status = topic.get("status")
        content_count = topic.get("content_count", 0)
        
        # 计算不活跃天数
        stats = TopicContentRepo.get_topic_content_stats(topic_id)
        last_content_at = stats.get("last_content_at")
        days_inactive = (now - last_content_at).days
        
        # 状态转换规则
        if status == "emerging" and content_count >= 10:
            new_status = "active"
        elif status in ("emerging", "active") and days_inactive >= 7:
            new_status = "declining"
        elif status == "declining" and days_inactive >= 30:
            new_status = "ended"
        
        if new_status:
            TopicEventRepo.update_status(topic_id, new_status)
```

#### 3.3.3.4.4 热度评估模型

**热度分数计算公式**：

```python
def _calculate_hot_score(delta: int, interaction: int) -> float:
    """计算热度分数"""
    return delta * 10.0 + interaction * 0.01
```

**热度等级判定**：

| 热度分数 | 等级 | 含义 |
|----------|------|------|
| >= 500 | critical | 危急级别，需要立即关注 |
| >= 100 | high | 高热度，需要重点关注 |
| >= 20 | medium | 中等热度，常规监控 |
| < 20 | low | 低热度，轻微关注 |

#### 3.3.3.4.5 统计更新

更新话题的聚合统计数据：

```python
def update_all_stats(self):
    """更新所有话题的聚合统计"""
    
    for topic in topics:
        # 1. 获取话题内容统计
        stats = TopicContentRepo.get_topic_content_stats(topic_id)
        
        # 2. 情感统计
        avg_sentiment = stats.get("avg_sentiment")
        sentiment_dist = {
            "positive": stats.get("positive_count", 0),
            "neutral": stats.get("neutral_count", 0),
            "negative": stats.get("negative_count", 0),
        }
        
        # 3. 主导情感判定
        dominant = max(sentiment_dist, key=sentiment_dist.get)
        if total > 0 and sentiment_dist[dominant] / total < 0.4:
            dominant = "mixed"
        
        # 4. 更新数据库
        TopicEventRepo.update_sentiment_stats(...)
        
        # 5. 热度和平台分布
        heat_level = self._calculate_heat_level(content_count, interaction)
        TopicEventRepo.update_heat_and_platform(...)
```

#### 3.3.3.4.6 演化快照生成

每日生成话题演化快照，记录话题的历史演化过程：

```python
def generate_evolution(self, snapshot_date: Optional[date] = None):
    """生成话题演化快照"""
    
    snapshot_date = snapshot_date or date.today()
    
    for topic in topics:
        # 1. 当日增量统计
        daily = TopicContentRepo.get_daily_stats(topic_id, snapshot_date)
        delta = daily.get("content_count_delta", 0)
        interaction = daily.get("interaction_count", 0)
        
        # 2. 总量统计
        stats = TopicContentRepo.get_topic_content_stats(topic_id)
        total_count = stats.get("content_count", 0)
        
        # 3. 热度分数
        hot_score = self._calculate_hot_score(delta, interaction)
        
        # 4. 插入演化快照
        TopicEvolutionRepo.insert(
            event_id=topic_id,
            snapshot_date=snapshot_date,
            content_count_delta=delta,
            content_count_total=total_count,
            hot_score=hot_score,
            ...
        )
```

## 3.3.4 LLM智能模块

### 3.3.4.1 话题命名器（llm/namer.py）

#### 3.3.4.1.1 功能描述

使用Qwen2.5大语言模型为话题生成描述性名称和摘要，替代基于关键词的简单命名方式。

#### 3.3.4.1.2 系统提示词设计

```python
SYSTEM_PROMPT = """你是一个专业的舆情监测分析师。请根据用户提供的多篇社交媒体内容，识别并总结其核心舆情话题。

请只输出 JSON 格式结果，不要有其他内容。JSON 格式如下:
{
    "event_name": "舆情话题名称(10-20字，如'小米SU7起火事故引发安全争议')",
    "event_description": "舆情描述(80-150字，包含：事件概述、涉及主体、公众关注焦点、舆论情感倾向)",
    "keywords": [{"word": "关键词", "weight": 0.9}]
}

要求:
- event_name 采用"主体+事件+影响"结构，简洁概括舆情焦点
- event_description 需涵盖：①事件背景 ②涉及品牌/人物/机构 ③公众态度与情绪 ④潜在风险或影响
- keywords 提取3-5个舆情核心关键词，weight反映其在舆情中的重要程度(0-1)
- 如果内容涉及负面舆情，需在描述中明确指出风险点"""
```

#### 3.3.4.1.3 命名流程

1. 获取话题下的代表性内容（按相似度排序，取前10条）
2. 构建内容摘要文本
3. 调用Qwen2.5模型生成命名结果
4. 解析JSON响应，更新数据库

### 3.3.4.2 话题分类器（classifier/llm_classifier.py）

#### 3.3.4.2.1 功能描述

使用Qwen2.5大语言模型为话题生成多维分类标签，包括行业分类、事件类型、受众范围、风险等级等。

#### 3.3.4.2.2 分类维度

| 维度 | 选项数 | 说明 |
|------|--------|------|
| 一级分类 | 15 | 行业/领域分类（社会事件、娱乐明星、科技产品等） |
| 二级分类 | ~50 | 细分领域 |
| 事件类型 | 11 | 事件类型（产品问题、服务纠纷、安全事件等） |
| 受众范围 | 4 | local/regional/national/global |
| 时间敏感度 | 4 | breaking/trending/normal/evergreen |
| 情感强度 | 4 | mild/moderate/intense/extreme |
| 争议程度 | 4 | low/medium/high/extreme |
| 风险等级 | 4 | safe/attention/warning/dangerous |

#### 3.3.4.2.3 分类流程

1. 获取话题信息（名称、描述、关键词、情感等）
2. 构建分类提示词
3. 调用Qwen2.5模型生成分类结果
4. 解析JSON响应，更新topic_classification表
5. 同步分类信息到topic_event表

## 3.3.5 命令行接口（cli/main.py）

### 3.3.5.1 可用命令

| 命令 | 功能 | 说明 |
|------|------|------|
| cluster | 增量聚类 | 对未聚类内容进行Single-Pass聚类 |
| stats | 统计信息 | 查看话题和内容的统计信息 |
| merge | 话题合并 | 合并相似话题 |
| evolve | 演化更新 | 更新生命周期、统计、生成演化快照 |
| describe | 话题命名 | 使用LLM为话题生成名称和描述 |
| wordcloud | 词云生成 | 生成话题词云数据 |
| classify | 话题分类 | 使用LLM为话题生成分类标签 |
| recluster | 全量重聚类 | 清除所有话题，重新聚类 |

### 3.3.5.2 使用示例

```bash
# 增量聚类
python -m TopicCluster cluster --threshold 0.75 --batch-size 64

# 查看统计
python -m TopicCluster stats

# 合并相似话题
python -m TopicCluster merge --threshold 0.92

# 更新演化快照
python -m TopicCluster evolve

# LLM话题命名
python -m TopicCluster describe --all

# LLM话题分类
python -m TopicCluster classify --only-unclassified
```

## 3.3.6 数据模型设计

### 3.3.6.1 topic_event（话题事件表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INT | 主键，自增 |
| event_name | VARCHAR(255) | 话题名称（LLM生成） |
| event_description | TEXT | 话题描述 |
| centroid_embedding | BLOB | 质心嵌入向量（768维，3072字节） |
| keywords | JSON | 关键词列表 |
| content_count | INT | 关联内容数量 |
| status | ENUM | 状态（emerging/active/declining/ended/merged） |
| similarity_threshold | FLOAT | 相似度阈值 |
| embedding_model | VARCHAR(255) | 嵌入模型名称 |
| avg_sentiment_score | FLOAT | 平均情感分数（-1到1） |
| sentiment_distribution | JSON | 情感分布 {positive, neutral, negative} |
| dominant_sentiment | VARCHAR(20) | 主导情感 |
| dominant_emotions | VARCHAR(255) | 主导情绪标签（逗号分隔） |
| heat_level | ENUM | 热度等级（low/medium/high/critical） |
| platform_distribution | JSON | 平台分布 |
| comment_count | INT | 评论总数 |
| wordcloud_data | JSON | 词云数据 |
| first_content_at | DATETIME | 首次内容时间 |
| last_content_at | DATETIME | 最后内容时间 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

### 3.3.6.2 topic_evolution（话题演化表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INT | 主键 |
| event_id | INT | 话题ID，外键 |
| snapshot_date | DATE | 快照日期 |
| content_count_delta | INT | 当日内容增量 |
| comment_count_delta | INT | 当日评论增量 |
| content_count_total | INT | 累计内容数 |
| avg_sentiment_score | FLOAT | 平均情感分数 |
| sentiment_distribution | JSON | 情感分布 |
| hot_score | FLOAT | 热度分数 |
| interaction_count | INT | 互动量 |
| keywords | JSON | 关键词 |
| platform_distribution | JSON | 平台分布 |
| created_at | DATETIME | 创建时间 |

### 3.3.6.3 topic_merge_log（话题合并日志表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INT | 主键 |
| source_event_id | INT | 源话题ID |
| target_event_id | INT | 目标话题ID |
| similarity_score | FLOAT | 相似度分数 |
| merge_reason | TEXT | 合并原因 |
| source_content_count | INT | 源话题内容数 |
| source_keywords | JSON | 源话题关键词 |
| merged_at | DATETIME | 合并时间 |

### 3.3.6.4 topic_classification（话题分类表）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INT | 主键 |
| topic_id | INT | 话题ID，外键，唯一 |
| primary_category | VARCHAR(50) | 一级分类 |
| secondary_category | VARCHAR(50) | 二级分类 |
| tertiary_category | VARCHAR(50) | 三级分类 |
| industry_tags | JSON | 行业标签列表 |
| event_type | VARCHAR(50) | 事件类型 |
| audience_scope | VARCHAR(20) | 受众范围 |
| time_sensitivity | VARCHAR(20) | 时间敏感度 |
| sentiment_intensity | VARCHAR(20) | 情感强度 |
| controversy_level | VARCHAR(20) | 争议程度 |
| risk_level | VARCHAR(20) | 风险等级 |
| risk_keywords | JSON | 风险关键词 |
| classification_confidence | FLOAT | 分类置信度 |
| classified_by | VARCHAR(20) | 分类来源（llm/manual） |
| classification_reason | VARCHAR(100) | 分类理由 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

## 3.3.7 关键技术亮点与创新

### 3.3.7.1 BERT + Faiss 高效聚类

创新性地将BERT语义嵌入与Faiss向量检索相结合：

1. **语义表示**：使用BERT的[CLS] token表示文本的语义，比TF-IDF等传统方法具有更强的语义理解能力，能够识别同义词、近义词

2. **高效检索**：Faiss的IndexFlatIP在768维向量上的检索效率极高，支持百万级话题的快速检索，查询时间复杂度为O(n)

3. **归一化技巧**：对嵌入向量进行L2归一化，使内积等价于余弦相似度，避免了复杂的相似度计算，同时保证数值稳定性

### 3.3.7.2 Running Average 质心更新

话题质心采用增量滑动平均更新策略：

| 方法 | 时间复杂度 | 空间复杂度 | 说明 |
|------|------------|------------|------|
| 传统方法 | O(n²) | O(n) | 每次更新质心需要遍历话题下所有内容 |
| Running Average | O(1) | O(1) | 只保存质心和计数 |

该优化使系统能够支持大规模流式数据处理，即使话题包含数万条内容，也能实现实时更新。

### 3.3.7.3 话题生命周期管理

设计完整的话题生命周期状态机模型：

1. **自动状态转换**：基于内容数量和不活跃天数自动更新话题状态，无需人工干预

2. **演化追踪**：通过演化快照记录话题的历史演化过程，支持回溯分析

3. **话题合并**：通过全局相似度扫描自动合并相似话题，减少信息冗余

### 3.3.7.4 多维特征融合

话题聚类不仅基于文本语义，还融合了多种维度的特征：

1. **文本语义特征**：BERT嵌入（768维）
2. **情感特征**：情感类别、情感分数、细粒度情绪标签
3. **传播特征**：点赞数、评论数、分享数、平台分布
4. **时间特征**：发布时间、时间密度、生命周期阶段
5. **热度特征**：热度分数、热度等级

### 3.3.7.5 LLM-Agent协同

系统在话题命名和分类环节引入大语言模型：

1. **话题命名Agent**：为话题生成描述性名称和摘要，替代简单的关键词拼接

2. **话题分类Agent**：为话题生成分类标签，包括行业分类、事件类型、风险等级等

3. **情感分析Agent**：利用Qwen2.5实现18种细粒度情绪识别（详见技术方案3.2）

## 3.3.8 算法伪代码详细

### 3.3.8.1 Single-Pass 增量聚类算法

```python
def single_pass_clustering(contents: List[Content], threshold: float) -> ClusterResult:
    """Single-Pass 增量聚类"""
    
    # 1. 加载活跃话题质心到索引
    index = load_topics_to_index()
    topic_counts = {t.id: t.content_count for t in topics}
    topic_centroids = {t.id: t.centroid for t in topics}
    
    result = ClusterResult()
    
    # 2. 遍历每条内容
    for content in contents:
        # 2.1 计算BERT嵌入
        embedding = bert_embed(content.text)
        
        # 2.2 搜索最相似话题
        matches = index.search(embedding, k=1)
        
        # 2.3 判断是否归入现有话题
        if matches and matches[0].similarity >= threshold:
            topic_id = matches[0].topic_id
            
            # 2.3.1 更新话题质心（Running Average）
            n = topic_counts[topic_id]
            old_centroid = topic_centroids[topic_id]
            new_centroid = (old_centroid * n + embedding) / (n + 1)
            new_centroid = normalize(new_centroid)
            
            topic_counts[topic_id] = n + 1
            topic_centroids[topic_id] = new_centroid
            index.update_centroid(topic_id, new_centroid)
            
            # 2.3.2 分配内容到话题
            assign_content_to_topic(content.id, topic_id, matches[0].similarity)
            result.assigned_to_existing += 1
        else:
            # 2.4 创建新话题
            topic_id = create_new_topic(embedding, content)
            assign_content_to_topic(content.id, topic_id, 1.0)
            result.new_topics_created += 1
        
        result.total_processed += 1
    
    # 3. 批量更新数据库
    batch_update_centroids(topic_centroids, topic_counts)
    batch_assign_topics(assignments)
    
    return result
```

### 3.3.8.2 话题合并算法

```python
def merge_topics(merge_threshold: float) -> MergeResult:
    """合并相似话题"""
    
    # 1. 加载所有活跃话题
    topics = load_active_topics()
    topic_map = {t.id: t for t in topics}
    index = build_index(topics)
    
    # 2. 计算所有话题对的相似度
    pairs = index.get_all_pairs_similarity()
    
    result = MergeResult()
    merged_ids = set()
    
    # 3. 按相似度降序遍历
    for topic_a, topic_b, similarity in pairs:
        if similarity < merge_threshold:
            break
        
        if topic_a in merged_ids or topic_b in merged_ids:
            continue
        
        # 4. 小话题合并到大话题
        if topic_a.content_count >= topic_b.content_count:
            target, source = topic_a, topic_b
        else:
            target, source = topic_b, topic_a
        
        # 5. 执行合并
        reassign_content(source.id, target.id)
        update_topic_status(source.id, "merged", target.id)
        log_merge_operation(source, target, similarity)
        
        merged_ids.add(source.id)
        result.merged_count += 1
    
    result.pairs_checked = len(pairs)
    return result
```

### 3.3.8.3 生命周期状态更新算法

```python
def update_lifecycle(inactive_days: int, ended_days: int) -> int:
    """更新话题生命周期状态"""
    
    topics = get_active_topics()
    now = datetime.now()
    updated = 0
    
    for topic in topics:
        status = topic.status
        content_count = topic.content_count
        days_inactive = calculate_inactive_days(topic.last_content_at, now)
        
        # 状态转换规则
        new_status = None
        if status == "emerging" and content_count >= 10:
            new_status = "active"
        elif status in ("emerging", "active") and days_inactive >= inactive_days:
            new_status = "declining"
        elif status == "declining" and days_inactive >= ended_days:
            new_status = "ended"
        
        if new_status:
            update_status(topic.id, new_status)
            updated += 1
    
    return updated
```

## 3.3.9 性能优化策略

### 3.3.9.1 批量处理优化

- 每次从数据库获取一批未聚类内容（默认batch_size=64）
- BERT嵌入批量计算，减少模型调用开销
- 数据库操作批量写入，减少IO次数

### 3.3.9.2 缓存机制

- 内存中缓存话题质心和计数
- 更新时先写缓存，定时同步到数据库
- 避免频繁的数据库读取

### 3.3.9.3 设备选择

- 自动检测GPU，可用时使用GPU加速BERT推理
- 禁用梯度计算（torch.no_grad()），减少显存占用
- 模型只读一次，常驻内存

### 3.3.9.4 索引优化

- Faiss索引采用内积索引，无需训练
- 定期重建索引，保持检索效率
- 使用L2归一化，保证数值稳定性

## 3.3.10 实验结果与分析

### 3.3.10.1 聚类效果

| 指标 | 数值 |
|------|------|
| 待聚类内容 | 10,000+条 |
| 话题数量 | 100+个 |
| 平均每话题内容数 | ~100条 |
| 合并率 | ~15% |
| 相似度阈值 | 0.75 |

### 3.3.10.2 处理性能

| 模块 | 处理速度 | 说明 |
|------|----------|------|
| BERT嵌入提取 | ~8条/秒（CPU） | GPU可提升10倍以上 |
| Faiss向量检索 | ~1000条/秒 | 索引内话题数<1000 |
| 话题聚类 | ~7条/秒（含嵌入） | 主要瓶颈在嵌入提取 |
| 话题合并 | O(n²) | 与话题数的平方成正比 |

### 3.3.10.3 话题生命周期分布

| 状态 | 占比 | 说明 |
|------|------|------|
| emerging | 30% | 新产生的话题 |
| active | 40% | 活跃讨论的话题 |
| declining | 20% | 热度下降的话题 |
| ended | 10% | 已结束的话题 |

## 3.3.11 应用场景

### 3.3.11.1 舆情监测

- 实时发现网络上的新话题事件
- 追踪话题的传播路径和演化趋势
- 识别高风险话题，及时预警
- 分析不同平台的传播差异

### 3.3.11.2 品牌管理

- 监控品牌相关话题的讨论热度
- 分析用户对品牌的态度变化
- 发现潜在的负面舆情
- 评估营销活动的效果

### 3.3.11.3 事件追踪

- 追踪社会热点事件的发展过程
- 分析事件的时间演化规律
- 生成事件演化报告
- 评估事件的影响范围

### 3.3.11.4 风险预警

- 基于风险等级的话题筛选
- 基于情感强度的话题监控
- 基于争议程度的话题预警

## 3.3.12 技术总结

本项目的增量聚类与事件组织方法，针对舆情分析的实时性和演化性需求，设计了一套完整的解决方案：

1. **高效性**：通过Running Average将质心更新复杂度从O(n²)降低到O(1)，支持大规模流式处理

2. **可扩展性**：Faiss索引支持大规模话题的快速检索，查询效率高

3. **智能化**：集成LLM-Agent，实现话题的自动命名和分类

4. **演化性**：完整的话题生命周期管理，支持话题的动态演化追踪

5. **准确性**：BERT语义嵌入比传统TF-IDF具有更强的语义理解能力

6. **实用性**：支持批量处理和增量处理两种模式，提供完善的CLI接口

该技术方案已经在实际项目中得到验证，能够有效支持舆情态势感知与推演干预平台的构建，累计处理内容超过10,000条，生成话题100+个，为舆情分析提供了有力的技术支撑。
